# Optional override to redirect all HTTP requests to HTTPS.
# Apply this after certificates/TLS routes are ready.
traefik:
  ports:
    web:
      http:
        redirections:
          entryPoint:
            to: websecure
            scheme: https
            permanent: true
