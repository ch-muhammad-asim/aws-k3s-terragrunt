apiVersion: v2
name: traefik-stack
description: Pinned Traefik ingress controller wrapper chart for the K3s EC2 cluster
type: application
version: 1.0.0
appVersion: "v3.7.12"
kubeVersion: ">=1.25.0-0"

dependencies:
  - name: traefik
    alias: traefik
    version: "41.4.0"
    repository: "https://traefik.github.io/charts"
