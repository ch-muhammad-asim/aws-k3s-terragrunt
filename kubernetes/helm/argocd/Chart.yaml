apiVersion: v2
name: argocd-platform
description: Pinned wrapper chart for deploying Argo CD on the K3s cluster.
type: application
version: 1.0.0
appVersion: "v3.5.2"

dependencies:
  - name: argo-cd
    alias: argocd
    version: "10.8.1"
    repository: "https://argoproj.github.io/argo-helm"
